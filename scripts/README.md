# Scripts

Utility scripts rarely used during development.

## migrate_world_tags.py

Migrates the world tag YAML data format.

Currently:
- adds missing description fields
- normalises YAML formatting

Requires:

    pip install ruamel.yaml
